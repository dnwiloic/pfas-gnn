__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
# NOTE: data/CA-PFAS-ASGWS.parquet is intentionally tracked in git so that a Colab
# `git clone` brings the dataset with the code (no Google Drive). Do not ignore it.
